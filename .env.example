# Postgres (matches docker-compose.yml)
DB_HOST=localhost
DB_PORT=5434
DB_NAME=mindspace
DB_USER=mindspace
DB_PASSWORD=mindspace

# Required for seeding embeddings and for POST /api/chat/ask
OPENAI_API_KEY=

PORT=8080
